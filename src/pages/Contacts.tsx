import { ContactsScreenReference } from "@/components/contacts/contacts-screen-reference";

export default function Contacts() {
  return (
    <div className="page-contacts">
      <ContactsScreenReference />
    </div>
  );
}